<Window x:Class="Pdv.Ui.Views.FinalizeSaleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Finalizar venda" Height="290" Width="430" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#EFF6FF" KeyDown="Window_KeyDown">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Finalização de pagamento" FontSize="22" FontWeight="Bold" Foreground="#1E3A8A"/>
        <TextBlock Grid.Row="1" Margin="0,8,0,0" Text="Escolha a forma de pagamento" Foreground="#334155"/>

        <Border Grid.Row="2" Margin="0,14,0,0" Background="White" CornerRadius="12" Padding="14">
            <StackPanel>
                <RadioButton x:Name="CashOption" Content="Dinheiro" IsChecked="True" Margin="0,0,0,8" FontSize="15"/>
                <RadioButton x:Name="CardOption" Content="Cartão" Margin="0,0,0,8" FontSize="15"/>
                <RadioButton x:Name="PixOption" Content="PIX" FontSize="15"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancelar (ESC)" Padding="14,8" Margin="0,0,8,0" Click="Cancel_Click" Background="#64748B" BorderBrush="#64748B"/>
            <Button Content="Confirmar (ENTER)" Padding="14,8" Click="Confirm_Click"/>
        </StackPanel>
    </Grid>
</Window>
