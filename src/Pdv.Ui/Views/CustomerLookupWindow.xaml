<Window x:Class="Pdv.Ui.Views.CustomerLookupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:converters="clr-namespace:Pdv.Ui.Converters"
        Title="Buscar cliente"
        Height="640"
        Width="980"
        WindowStartupLocation="CenterOwner"
        Background="#F8FAFC"
        KeyDown="Window_KeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBooleanConverter x:Key="InverseBoolConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Busca de clientes" FontSize="24" FontWeight="Bold" Foreground="#0F172A"/>

        <Grid Grid.Row="1" Margin="0,14,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="QueryTextBox"
                     Height="40"
                     VerticalContentAlignment="Center"
                     FontSize="16"
                     Padding="12,0"
                     Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Grid.Column="1"
                    Content="Cadastrar cliente"
                    Margin="12,0,0,0"
                    Padding="16,10"
                    Background="#7C3AED"
                    BorderBrush="#7C3AED"
                    Click="CreateCustomer_Click"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
            <Button Grid.Column="2"
                    Content="Selecionar cliente"
                    Margin="12,0,0,0"
                    Padding="16,10"
                    Click="SelectCustomer_Click"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBoolConverter}}"/>
        </Grid>

        <Border Grid.Row="2" Background="White" CornerRadius="14" Padding="12">
            <DataGrid x:Name="CustomersDataGrid"
                      ItemsSource="{Binding Customers}"
                      SelectedItem="{Binding SelectedCustomer}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      MouseDoubleClick="CustomersDataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="CPF" Binding="{Binding Cpf}" Width="180"/>
                    <DataGridTextColumn Header="Telefone" Binding="{Binding Phone}" Width="170"/>
                    <DataGridTextColumn Header="E-mail" Binding="{Binding Email}" Width="220"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,14,0,0">
            <ProgressBar Height="6" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
            <TextBlock Text="{Binding StatusMessage}" Margin="0,8,0,0" Foreground="#334155" TextWrapping="Wrap"/>
        </StackPanel>
    </Grid>
</Window>
