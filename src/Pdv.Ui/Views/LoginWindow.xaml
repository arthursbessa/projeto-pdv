<Window x:Class="Pdv.Ui.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login do PDV" Height="430" MinHeight="430" Width="540" WindowStartupLocation="CenterScreen" Background="#0F172A">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>

    <Grid Margin="30">
        <Border Background="#111827" CornerRadius="16" Padding="24">
            <StackPanel>
                <TextBlock Text="Acesso ao Sistema PDV" FontSize="28" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Usuário (e-mail)" Margin="0,20,0,6" Foreground="#BFDBFE"/>
                <TextBox Text="{Binding Username}" Height="36"/>
                <TextBlock Text="Senha" Margin="0,10,0,6" Foreground="#BFDBFE"/>
                <PasswordBox x:Name="PasswordInput" Height="36" PasswordChanged="PasswordInput_OnPasswordChanged"/>
                <Button Content="Entrar" Height="40" Margin="0,18,0,0" Click="Login_Click" Background="#2563EB" Foreground="White"/>
                <ProgressBar Height="6" Margin="0,10,0,0" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"/>
                <TextBlock Text="{Binding StatusMessage}" Margin="0,14,0,0" Foreground="#FDE68A"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
